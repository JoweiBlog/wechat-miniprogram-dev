<!--index.wxml-->
<view class="container">

  <navigator wx:for="{{spec}}" wx:key="index" class='item' url='{{item.url}}'>
    <view class='title'>{{item.title}}</view>
    <view class='desc'>{{item.desc}}</view>
  </navigator>

</view>
